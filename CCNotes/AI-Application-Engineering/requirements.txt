# AI-Application-Engineering 依赖清单
# 环境：复用 conda agent_env（解释器 E:\software\OfficeWorkLife\Anaconda\envs\agent_env\python.exe）
# 若在新环境安装：pip install -i https://pypi.tuna.tsinghua.edu.cn/simple -r requirements.txt

# ── 阶段1 生产级 RAG + API 服务 ──
fastapi>=0.133
uvicorn>=0.41
pydantic>=2.12
httpx>=0.28
chromadb>=1.5
python-dotenv
langchain>=1.3
langchain-openai>=1.3
langgraph>=1.2

# ── 阶段5 工程化（PostgreSQL 持久化）──
psycopg2-binary>=2.9
# redis>=5            # 本机缺，需时装（缓存阶段降级为内存/文件）

# ── 阶段6 微调（本机 RTX 4060 8GB，需时装）──
# peft
# transformers
# datasets
# accelerate
# bitsandbytes        # Windows 支持有限，QLoRA 视情况
